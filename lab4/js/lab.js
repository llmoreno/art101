//Generator creates unique code for users to connect to.
    //Code appears in form of link, or typeable form
//Generator has two input forms, one that accepts the code to connect, one that accepts the word cloud submission.
//Generator sorts inputted words.
    //Words that are repeated are combined.
    //Words with spaces between are treated as separate submissions.
    //Words with commas are treated as one submission.
//Generator creates image of submitted words.
    //Generator assigns random orientation and color to each word, excluding upside down orientation.
    //Words that were repeated appear larger in the word cloud than words that were typed only once.
//Generator gives option to save output as image file.
